// Fetch data from the backend API endpoint
fetch('/api/data')
  .then(response => response.json())
  .then(data => {
    // Use the received data for visualization
    console.log(data); // Example: { labels: [...], values: [...] }
    // Implement code to render charts/graphs using the received data
  })
  .catch(error => {
    console.error('Error fetching data:', error);
  });
